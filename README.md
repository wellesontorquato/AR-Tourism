# AR Turismo Cultural (MVP)

MVP full-stack:
- Next.js (App Router) + TypeScript + Tailwind
- Prisma + SQLite
- PWA (manifest + service worker)
- App do turista: câmera + GPS + bússola (quando disponível)
- Admin: login por senha (env) + CRUD de POIs

## 1) Setup

1) Instale dependências:
```bash
npm install
